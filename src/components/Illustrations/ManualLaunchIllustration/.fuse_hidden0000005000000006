@use 'src/styles/mixins' as m;
@use 'src/styles/variables' as v;

// ---------------------------------------------------------------------------
// Keyframes — prefixed with ml- to avoid global name collisions
// ---------------------------------------------------------------------------
@keyframes ml-fadeIn {
  from {
    opacity: 0;
  }

  to {
    opacity: 1;
  }
}

@keyframes ml-slideUp {
  from {
    opacity: 0;
    transform: translateY(10px);
  }

  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes ml-slideIn {
  from {
    opacity: 0;
    transform: translateX(-8px);
  }

  to {
    opacity: 1;
    transform: translateX(0);
  }
}

@keyframes ml-tagSlide {
  from {
    opacity: 0;
    transform: translateX(-10px);
  }

  to {
    opacity: 1;
    transform: translateX(0);
  }
}

@keyframes ml-sectionUp {
  from {
    opacity: 0;
    transform: translateY(14px);
  }

  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes ml-popIn {
  from {
    opacity: 0;
    transform: scale(0.78);
  }

  to {
    opacity: 1;
    transform: scale(1);
  }
}

@keyframes ml-btnClick {
  0% {
    transform: scale(1);
  }

  35% {
    transform: scale(0.9);
    filter: brightness(0.88);
  }

  100% {
    transform: scale(1);
  }
}

@keyframes ml-dotPulse {
  0%,
  100% {
    opacity: 1;
  }

  50% {
    opacity: 0.35;
  }
}

// ---------------------------------------------------------------------------
// Root block
// ---------------------------------------------------------------------------
.manual-launch {
  width: 100%;
  max-width: 760px;
  margin: 0 auto;
  text-align: left;
  line-height: 1.25; // illu-base — illustrations were tuned to this; website base lh differs
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;

  // ---------------------------------------------------------------------------
  // Scene wrapper
  // ---------------------------------------------------------------------------
  &__scene {
    display: flex;
    border: 0.5px solid #c8dde6;
    border-radius: 14px;
    box-shadow: 0 4px 28px rgb(0 80 120 / 0.12);
    opacity: 0;
    overflow: hidden;

    &--active {
      animation: ml-fadeIn 0.4s ease both 0.05s;
    }

    // Scale the 760px-wide scene to fit the mobile container
    @media (width <= 767px) {
      width: 760px;
      zoom: calc((100vw - 32px) / 760px);
    }
  }

  // Header — triggered by --active on scene
  &__scene--active &__hdr {
    animation: ml-slideUp 0.4s ease both 0.15s;
  }

  // Steps section — triggered by --active on scene
  &__scene--active &__steps-section {
    animation: ml-slideUp 0.38s ease both 0.25s;
  }

  &__hdr {
    position: relative;
    display: flex;
    flex-shrink: 0;
    align-items: center;
    gap: 10px;
    min-height: 48px;
    border-bottom: 0.5px solid #d8e4ee;
    padding: 10px 18px;
    background: #fff;
  }

  // ---------------------------------------------------------------------------
  // Nav icon button
  // ---------------------------------------------------------------------------
  &__nb {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 28px;
    height: 24px;
    border-radius: 5px;

    &--on {
      background: rgb(0 180 213 / 0.2);
    }
  }

  // ---------------------------------------------------------------------------
  // Section card (shared by steps / bts / comment)
  // ---------------------------------------------------------------------------
  &__section {
    border: 0.5px solid #e0eaf2;
    border-radius: 8px;
    box-shadow: 0 1px 4px rgb(0 80 120 / 0.05);
    background: #fff;
    overflow: hidden;
  }

  // ---------------------------------------------------------------------------
  // Steps section (always visible, CSS-animated)
  // ---------------------------------------------------------------------------
  &__steps-section {
    // opacity starts at 0 via animation initial state (both fill-mode)
  }

  // ---------------------------------------------------------------------------
  // Step rows — JS-driven
  // ---------------------------------------------------------------------------
  &__step-row {
    display: grid;
    align-items: center;
    grid-template-columns: 36px 1fr 1fr 28px;
    border-top: 0.5px solid #eef3f8;
    padding: 9px 14px;
    opacity: 0;

    &--visible {
      animation: ml-slideIn 0.32s ease forwards;
    }
  }

  // ---------------------------------------------------------------------------
  // Step icon — JS-driven
  // ---------------------------------------------------------------------------
  &__step-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    transform: scale(0.4);
    transition: opacity 0.22s cubic-bezier(0.34, 1.56, 0.64, 1),
      transform 0.22s cubic-bezier(0.34, 1.56, 0.64, 1);

    &--visible {
      opacity: 1;
      transform: scale(1);
    }
  }

  // ---------------------------------------------------------------------------
  // Failed pill — click animation
  // ---------------------------------------------------------------------------
  &__s-fail {
    transition: transform 0.15s, filter 0.15s;

    &--clicking {
      animation: ml-btnClick 0.26s ease forwards;
    }
  }

  // ---------------------------------------------------------------------------
  // Execution chip (shows after pills fade)
  // ---------------------------------------------------------------------------
  &__exec-chip {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    border-radius: 6px;
    padding: 4px 10px;
    box-shadow: 0 2px 8px rgb(200 55 58 / 0.3);
    background: #c8373a;
    animation: ml-popIn 0.3s cubic-bezier(0.34, 1.56, 0.64, 1) both;
    color: #fff;
    font-size: 11px;
    font-weight: 800;
  }

  &__exec-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: rgb(255 255 255 / 0.7);
    animation: ml-dotPulse 2s ease-in-out infinite;
  }

  // ---------------------------------------------------------------------------
  // BTS section — JS-driven
  // ---------------------------------------------------------------------------
  &__bts-section {
    opacity: 0;

    &--visible {
      animation: ml-sectionUp 0.4s cubic-bezier(0.22, 0.68, 0, 1.1) forwards;
    }
  }

  &__bts-tag {
    display: inline-flex;
    align-items: center;
    gap: 5px;
    border: 0.5px solid #b2e4ee;
    border-radius: 4px 8px 8px 4px;
    padding: 4px 8px;
    background: #ebf8fb;
    opacity: 0;
    color: #0081a7;
    font-size: 10px;
    font-weight: 700;
    cursor: default;

    &--visible {
      animation: ml-tagSlide 0.32s cubic-bezier(0.22, 0.68, 0, 1.1) forwards;
    }
  }

  &__bts-add {
    display: flex;
    align-items: center;
    gap: 4px;
    margin-left: auto;
    opacity: 0;
    color: #009dbb;
    font-size: 10px;
    font-weight: 700;
    cursor: default;

    &--visible {
      animation: ml-fadeIn 0.3s ease forwards;
    }
  }

  // ---------------------------------------------------------------------------
  // Comment section — JS-driven
  // ---------------------------------------------------------------------------
  &__comment-section {
    opacity: 0;

    &--visible {
      animation: ml-sectionUp 0.4s cubic-bezier(0.22, 0.68, 0, 1.1) forwards;
    }
  }

  // ---------------------------------------------------------------------------
  // Reduced-motion overrides
  // ---------------------------------------------------------------------------
  @media (prefers-reduced-motion: reduce) {
    &__scene,
    &__step-row,
    &__bts-section,
    &__bts-tag,
    &__bts-add,
    &__comment-section {
      opacity: 1 !important;
      transform: none !important;
      animation: none !important;
    }

    &__step-icon {
      opacity: 1 !important;
      transform: scale(1) !important;
      transition: none !important;
    }

    &__exec-chip,
    &__exec-dot {
      animation: none !important;
    }

    &__s-fail--clicking {
      animation: none !important;
    }
  }

  // ---------------------------------------------------------------------------
  // Mobile — show everything immediately, no animation
  // ---------------------------------------------------------------------------
  @media (width <= 767px) {
    &__scene,
    &__step-row,
    &__bts-section,
    &__bts-tag,
    &__bts-add,
    &__comment-section {
      opacity: 1 !important;
      transform: none !important;
      animation: none !important;
    }

    &__step-icon {
      opacity: 1 !important;
      transform: scale(1) !important;
      transition: none !important;
    }

    &__exec-chip,
    &__exec-dot {
      animation: none !important;
    }

    &__s-fail--clicking {
      animation: none !important;
    }
  }
}
